

Run the project it using live-server. You can install that using - npm install -g live-server. Run server on a random port binding to all network interfaces.

X axis represent time. Y axis represent tone (Swar).

The solution must be implemented in single index.html file. 